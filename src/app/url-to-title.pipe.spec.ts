import { UrlToTitlePipe } from './url-to-title.pipe';

describe('UrlToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
